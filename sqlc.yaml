version: "2"
sql:
    - engine: "postgresql"
      name: "db" # name of go package generated output
      schema: "./db/migration/"
      queries: "./db/query/"
      gen:
          go:
              package: "db"
              out: "db/sqlc"
              sql_package: "pgx/v5"
              emit_json_tags: true
              emit_interface: true
              emit_empty_slices: true # not return nil
              emit_exact_table_names: false
              emit_prepared_queries: true # true : prevent sql injection, cache statement id in db for each connection, combine with pool connection
